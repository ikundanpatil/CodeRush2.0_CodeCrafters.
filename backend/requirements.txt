fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
pytest>=7.0.0
requests>=2.28.0

# Phase 3 - Research memory
SQLAlchemy>=2.0.0
pymysql>=1.1.0
# chromadb is used for semantic memory; the app degrades to an in-memory
# vector fallback if it cannot be installed or is not running.
chromadb>=0.4.0

# Phase 1 - LLM adapter (OpenAI + NVIDIA NIM, which is OpenAI-compatible)
openai>=1.0.0

# Phase 2 - Web search (Tavily) + safe browser use `requests` (already above)
# and the stdlib only -- no extra dependency needed.

# Phase 4 - Evidence graph persistence reuses SQLAlchemy/pymysql (already
# above) with the same MySQL database as Phase 3 memory.
